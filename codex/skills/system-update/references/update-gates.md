# Update gates

Treat firmware, bootloader, kernel, storage stack, encryption, remote access,
identity, database, and major-version updates as high impact. Confirm power and
recovery access for firmware/boot work and avoid remote-only execution when a
lockout could prevent recovery. Never switch repositories or trust keys merely
to obtain a newer version without separate approval and provenance review.
