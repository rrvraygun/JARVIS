# Cleanup policy

Do not remove user documents, active application data, unknown files, backups,
current kernels, package databases, security logs under retention, or recovery
artifacts merely because they are large or old. Separate caches, logs, temporary
files, packages, containers, snapshots, and user data; each needs its own owner
and retention rule. A cleanup that cannot enumerate exact targets is not ready.
